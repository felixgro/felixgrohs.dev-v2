{
	"name": "felixgrohs",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "lerna run build",
		"dev:plain": "cd packages/client && yarn dev:plain",
		"dev": "netlify dev",
		"lint": "lerna run lint",
		"test": "lerna run test"
	},
	"devDependencies": {
		"lerna": "^4.0.0",
		"netlify-cli": "^8.13.0"
	}
}
